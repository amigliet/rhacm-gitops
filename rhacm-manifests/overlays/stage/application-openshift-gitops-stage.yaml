apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: argocd-stage
  namespace: openshift-gitops
  labels:
    acm-app: argocd-stage
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: acm-app
        operator: In
        values:
          - argocd-stage
